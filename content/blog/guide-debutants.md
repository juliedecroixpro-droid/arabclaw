---
title: "دليل المبتدئين لـ OpenClaw"
excerpt: "تعلم كيفية استخدام OpenClaw من الصفر - دليل شامل للمبتدئين بالعربية"
date: "2026-02-07"
author: "فريق ArabClaw"
---

# دليل المبتدئين لـ OpenClaw

مرحباً بك في عالم OpenClaw! إذا كنت جديداً على هذه الأداة القوية، فأنت في المكان الصحيح.

## ما هو OpenClaw؟

OpenClaw هو مساعد ذكاء اصطناعي يعمل من سطر الأوامر. يمكنك استخدامه للتحدث مع نماذج AI مختلفة (Claude، GPT-4، وغيرها) مباشرة من Terminal.

**لماذا OpenClaw؟**

- **سرعة فائقة**: لا حاجة لفتح متصفح أو تطبيق
- **قوة كبيرة**: يمكنه تنفيذ أوامر، قراءة ملفات، التحكم بالمتصفح
- **مرونة**: دعم لعشرات نماذج AI المختلفة
- **مجاني**: مفتوح المصدر بالكامل

## التثبيت السريع

أسهل طريقة للبدء:

```bash
curl -fsSL https://openclaw.com/install.sh | bash
```

بعد التثبيت، شغّل:

```bash
openclaw
```

## أول خطواتك

### 1. التكوين الأساسي

عند أول تشغيل، سيطلب منك OpenClaw:

- **API Key**: مفتاح API لنموذج AI (مثلاً Anthropic أو OpenAI)
- **النموذج الافتراضي**: أي نموذج تريد استخدامه
- **قناة الدردشة**: Telegram، Discord، أو Terminal فقط

### 2. أول محادثة

جرب هذا:

```bash
openclaw chat "ما هي عاصمة مصر؟"
```

أو ادخل وضع المحادثة التفاعلية:

```bash
openclaw
```

ثم اكتب أسئلتك مباشرة.

### 3. استخدام Skills

Skills هي قدرات إضافية يمكن لـ OpenClaw استخدامها. مثلاً:

```bash
# البحث في الإنترنت
"ابحث عن أحدث أخبار التكنولوجيا"

# التحكم بالمتصفح
"افتح Google واكتب تقرير الطقس"

# قراءة ملفات
"اقرأ محتوى ملف README.md"
```

## نصائح للمبتدئين

### استخدم اللغة الطبيعية

لا تحتاج لأوامر معقدة. تحدث بشكل طبيعي:

```
"ساعدني في كتابة email احترافي لمديري"
"ما الفرق بين Python و JavaScript؟"
"اعمل لي ملخص لهذا المقال: [رابط]"
```

### جرب نماذج مختلفة

OpenClaw يدعم عشرات النماذج:

- **Claude Sonnet 4.5**: سريع ورخيص (موصى به)
- **GPT-4**: قوي جداً للمهام المعقدة
- **Gemini**: مجاني من Google

غيّر النموذج في أي وقت:

```bash
/model claude-sonnet-4.5
```

### احفظ المحادثات

استخدم الذاكرة:

```
"احفظ هذا في الذاكرة: أنا أفضل Python على JavaScript"
```

في محادثات لاحقة، سيتذكر OpenClaw تفضيلاتك!

## الأوامر المفيدة

| الأمر | الوصف |
|-------|-------|
| `/status` | عرض حالة الجلسة |
| `/model <name>` | تغيير النموذج |
| `/clear` | مسح المحادثة |
| `/help` | عرض المساعدة |
| `/exit` | الخروج |

## المشاكل الشائعة

### "command not found"

أعد فتح Terminal أو شغّل:

```bash
source ~/.bashrc
```

### "API key invalid"

تأكد من أن مفتاح API صحيح:

```bash
openclaw config set anthropic.apiKey "sk-ant-..."
```

### بطء في الردود

جرب نموذج أسرع:

```bash
/model claude-sonnet-4.5
```

## الخطوات التالية

الآن بعد أن تعلمت الأساسيات:

1. **استكشف [التوثيق الكامل](https://docs.arabclaw.com)**
2. **جرب [Skills من Marketplace](https://marketplace.arabclaw.com)**
3. **انضم [لمجتمعنا على Discord](https://discord.gg/openclaw)**

## خلاصة

OpenClaw أداة قوية ومرنة. الطريقة الأفضل لتتعلمها هي التجربة! جرب أوامر مختلفة، استكشف Skills، وتفاعل مع المجتمع.

مرحباً بك في عائلة OpenClaw! 🎉

---

**أسئلة؟** تواصل معنا على [Discord](https://discord.gg/openclaw) أو [GitHub](https://github.com/openclaw/openclaw)
