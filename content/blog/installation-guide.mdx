---
title: "كيفية تثبيت OpenClaw في 5 دقائق"
excerpt: "دليل سريع وشامل لتثبيت OpenClaw على جهازك - خطوة بخطوة بالعربية"
date: "2025-02-06"
---

# كيفية تثبيت OpenClaw في 5 دقائق ⚡

هل تريد البدء باستخدام OpenClaw لكن لا تعرف من أين تبدأ؟ لا تقلق! في هذا الدليل، سأريك كيف تثبت OpenClaw على جهازك في أقل من 5 دقائق.

## لماذا OpenClaw؟

قبل أن نبدأ، دعني أخبرك لماذا OpenClaw رائع:

- **قوي وسريع**: استخدم أحدث نماذج الذكاء الاصطناعي من سطر الأوامر
- **مرن**: أضف Skills وخصص الأداة حسب احتياجاتك
- **مفتوح المصدر**: مجاني بالكامل ومفتوح المصدر

## المتطلبات

قبل التثبيت، تأكد من توفر:

- نظام تشغيل: macOS، Linux، أو Windows (مع WSL)
- اتصال بالإنترنت
- صلاحيات المسؤول (sudo) في بعض الحالات

## الطريقة 1: التثبيت السريع (موصى به)

هذه هي أسهل وأسرع طريقة للتثبيت:

```bash
curl -fsSL https://openclaw.com/install.sh | bash
```

هذا الأمر سيقوم بـ:
1. تحميل آخر نسخة من OpenClaw
2. تثبيتها تلقائياً
3. إضافتها لـ PATH
4. إعداد كل شيء للاستخدام المباشر

## الطريقة 2: التثبيت عبر npm

إذا كنت تفضل استخدام npm (يتطلب Node.js 18+):

```bash
npm install -g openclaw
```

## الطريقة 3: التثبيت عبر Docker

للذين يفضلون Docker:

```bash
docker pull openclaw/openclaw:latest
docker run -it openclaw/openclaw
```

## التحقق من التثبيت

بعد التثبيت، تحقق من نجاح العملية:

```bash
openclaw --version
```

إذا رأيت رقم الإصدار، مبروك! 🎉 التثبيت نجح.

## الخطوات التالية

الآن بعد أن ثبّت OpenClaw، إليك ما يمكنك فعله:

1. **جرّب أول أمر**: `openclaw chat "مرحباً"`
2. **اكتشف الـ Skills**: زر [Marketplace](https://marketplace.arabclaw.com)
3. **اقرأ التوثيق**: [docs.arabclaw.com](https://docs.arabclaw.com)

## مشاكل شائعة وحلولها

### المشكلة: "command not found"

إذا واجهت هذا الخطأ:

```bash
# أعد تحميل ملف التكوين
source ~/.bashrc
# أو
source ~/.zshrc
```

### المشكلة: أخطاء صلاحيات

إذا واجهت أخطاء صلاحيات:

```bash
sudo npm install -g openclaw
```

## الخلاصة

تثبيت OpenClaw سهل وسريع! اختر الطريقة التي تناسبك وابدأ رحلتك مع الذكاء الاصطناعي اليوم.

هل واجهت أي مشكلة؟ انضم لمجتمعنا على [Discord](https://discord.gg/openclaw) واطرح سؤالك!

---

**كلمات مفتاحية**: تثبيت OpenClaw، دليل المبتدئين، ذكاء اصطناعي بالعربية
